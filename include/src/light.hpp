#include "src/transform.h"
#include "src/objects.h"
#include <map>
#include <vector>

#ifndef LIGHT_H
#define LIGHT_H
/*
class Pixel_Cooridinates
{
public:
    int x, y;
};

class LightManager
{
public:
    vec3 I_diffuse;

    LightManager();
    std::map <Pixel_Cooridinates, std::vector<*Object>> light_map;
    vec3 find_light_ray(vec3, vec3);
    vec3 calculate_diffuse_intensity(vec3, vec3);
};
*/
#endif
